/**
 * @file Reply interface.
 */
export interface Reply {
    commentId: string;
    createdAt: any;
    docId: string;
    email: string;
    feedbackId: string;
    id: string;
    profilePicture: string;
    text: string;
    userId: string;
    userName: string;
}
