/**
 * @file Comment interface.
 */
export interface Comment {
    commentEmail: string;
    docId: string;
    email: string;
    feedbackId: string;
    id: string;
    profilePicture: string;
    text: string;
    userId: string;
    userName: string;
}
