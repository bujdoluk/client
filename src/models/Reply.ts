/**
 * @file Reply interface.
 */
export interface Reply {
    commentId: string;
    docId: string;
    email: string;
    feedbackId: string;
    profilePicture: string;
    text: number;
    userId: string;
    userName: string;
}
