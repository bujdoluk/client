/**
 * @file Comment interface.
 */
export interface Comment {
    createdAt: any;
    docId: string;
    email: string;
    feedbackId: string;
    picture: string;
    text: string;
    userId: string;
    userName: string;
}
