rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: open read/write for development — restrict before going to production
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 8, 12);
    }
  }
}
